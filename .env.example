# Copia este archivo a .env y rellena valores
# DATABASE_URL para Supabase (elige pooler transaction para serverless)
# Ejemplo transaction pooler:
# DATABASE_URL=postgresql://postgres.<ref>:[PASSWORD]@aws-1-<region>.pooler.supabase.com:6543/postgres
# Si usas direct connection:
# DATABASE_URL=postgresql://postgres:[PASSWORD]@db.<ref>.supabase.co:5432/postgres

# Forzar SSL (true para Supabase/Neon)
PGSSL=true

# Mercado Pago
MP_ACCESS_TOKEN=REEMPLAZAR
MP_WEBHOOK_SECRET=REEMPLAZAR
GRANT_SECRET=REEMPLAZAR

# Firebase Admin (service account recortado a claves necesarias)
FIREBASE_PROJECT_ID=tu_project_id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk@tu_project_id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nLINEAS\n-----END PRIVATE KEY-----\n"

# Base URL override (opcional)
# MP_BASE_URL=https://tu-dominio

# SMTP (para emails transaccionales). Si usas Google Workspace, configura SMTP Relay o usa smtp.gmail.com
# Opción Workspace SMTP Relay (recomendado):
# SMTP_HOST=smtp-relay.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=usuario@deceroacien.app
# SMTP_PASS=APP_PASSWORD_O_AUTH
# SMTP_FROM="DE CERO A CIEN <no-reply@deceroacien.app>"

# Opción Gmail SMTP directo (menos recomendado para producción):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=usuario@deceroacien.app
# SMTP_PASS=APP_PASSWORD
